# MenuIcons — NOT AVAILABLE ON macOS

**Identifier**: `menuicons`
**Status**: Cannot be meaningfully ported to macOS

## Reason

macOS NSMenuItem does not support icons in menu items. Windows-specific UI feature.

## Alternative

No macOS equivalent. macOS menus don't display icons.
