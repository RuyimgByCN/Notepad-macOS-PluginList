# Session Manager — BUILT-IN

**Identifier**: `sessionmgr`
**Status**: NotepadMac already has this feature built-in

## Built-in Feature

Session persistence (auto-save/restore open files)

## How to Access

NotepadMac automatically persists sessions

No plugin installation needed — this functionality is already part of NotepadMac.
