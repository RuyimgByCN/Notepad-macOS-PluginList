# Dark Mode C — BUILT-IN

**Identifier**: `dark_mode_c`
**Status**: NotepadMac already has this feature built-in

## Built-in Feature

macOS native dark/light mode switching via system appearance

## How to Access

System Preferences → Appearance, or NotepadMac → Preferences → Theme

No plugin installation needed — this functionality is already part of NotepadMac.
